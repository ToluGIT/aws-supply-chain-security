# Vulnerable variant — deliberately pinned to old versions with known recent
# CVEs, chosen to be pure-Python so they install on python:3.12-slim without a
# build toolchain (C-extension packages like aiohttp/Pillow/cryptography have
# no 3.12 wheels at these old versions and would fail to compile, breaking the
# build before Inspector ever runs). Inspector flags all of these; the CVE gate
# fails the build. This is the Phase 4 "CVE gate" test (threat T2).
# DO NOT use these versions for anything real.
urllib3==1.26.4          # CVE-2023-43804, CVE-2024-37891 (proxy/cookie auth leak)
requests==2.25.1         # CVE-2023-32681, CVE-2024-35195
Jinja2==3.0.0            # CVE-2024-22195, CVE-2024-34064
gunicorn==20.1.0         # CVE-2024-1135 (HTTP request smuggling)
Werkzeug==2.0.3          # CVE-2024-34069 (debugger RCE)
PyYAML==5.3.1            # CVE-2020-14343 (arbitrary code execution via full_load)
certifi==2022.12.7       # CVE-2023-37920 (removed e-Tugra root trust)
idna==2.8                # CVE-2024-3651 (resource exhaustion)
Flask==2.0.3             # pulls the vulnerable Werkzeug/Jinja2 chain
